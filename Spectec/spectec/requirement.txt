sudo apt-get update
sudo apt-get install -y opam ocaml m4 pkg-config libgmp-dev python3 python3-pip

opam init -y
eval $(opam env)

opam switch create 5.0.0
eval $(opam env)

opam install -y dune menhir mdx zarith


cd spectec/interpreter
make
